---
const { center, uppercase } = Astro.props
---

<h1 class={` ${ center ? "text-center" : "" } ${ uppercase ? "uppercase" : "" } text-thoughtbot_black font-calibre_semibold font-bold text-4xl md:text-6xl`}>
    <slot/>
</h1>
