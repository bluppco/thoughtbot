---
const { center, uppercase } = Astro.props
---

<h1 class={` ${ center ? "text-center" : "" } ${ uppercase ? "uppercase" : "" } text-thoughtbot_black font-calibre_medium text-3xl md:text-5xl font-semibold`}>
    <slot/>
</h1>
