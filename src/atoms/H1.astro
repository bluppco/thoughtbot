---
const { center, uppercase } = Astro.props
---
<h1 class={` ${ center ? "text-center" : "" } ${ uppercase ? "uppercase" : "" } text-thoughtbot_black font-calibre text-[40px] md:text-[56px] font-semibold leading-[1] tracking-t`}>
    <slot/>
</h1>
