---
const { center, uppercase, tracking_wide } = Astro.props
---
<h4 class={` ${ center ? "text-center" : "" } ${ uppercase ? "uppercase" : "" } ${ tracking_wide ? "tracking-widest" : "tracking-tighter" } text-thoughtbot_black font-calibre font-semibold text-[34px] md:text-4xl leading-9`}>
    <slot/>
</h4>
